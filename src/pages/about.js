import React from "react";
import Helmet from "react-helmet";

export default () => (
  <div>
    <Helmet title={`About: Russell Schmidt`} />
    <h1>About</h1>
    <p>
      About
    </p>
  </div>
);
